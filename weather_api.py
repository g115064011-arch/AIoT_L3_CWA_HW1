# weather_api.py
# This module will handle all interactions with the CWA (Central Weather Administration)
# Open Data API, including fetching weather forecast data for Taiwan's counties and cities.
# It will use the 'requests' library to make HTTP GET requests to the API endpoints,
# and the API key will be loaded securely from the .env file using python-dotenv.
